import { StyleSheet } from 'react-native'
import Constants from 'expo-constants'

export const datafetcherStyleSheet = StyleSheet.create({
  wrapper: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center'
  },
  listItem: {
    borderRadius: 6,
    marginBottom: 10,
    padding: 20,
    backgroundColor: '#c5cae9'
  }
})
